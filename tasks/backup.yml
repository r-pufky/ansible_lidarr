---
###############################################################################
# Backup
###############################################################################
# Only backup SQLite databases and configuration file.

- name: 'Backup | fact | parse location'
  ansible.builtin.set_fact:
    lidarr__archive: '{{ "/tmp/" ~ lidarr__srv._start ~ "_config.tar.xz" }}'

- name: 'Backup | warning | ⚠ Metadata Enabled ⚠'
  when: lidarr__cfg._backup_metadata
  ansible.builtin.debug:
    msg: |

      ⚠ Metadata Enabled ⚠

      /var/opt/lidarr/MediaCover may be Multi-GB. A backup effectively doubles
      the space used during the process and may fail if low disk space.

      Transfer may be very long.


- name: 'Backup | filesystem | set backup location'
  when:
    lidarr__cfg._backup_d | length > 0 and lidarr__cfg._backup_d != '/tmp'
  delegate_to: 'localhost'
  run_once: true
  ansible.builtin.file:
    path: '{{ lidarr__cfg._backup_d }}'
    mode: '0750'
    state: directory

- name: '{{  "Backup | cmd | create " ~ lidarr__archive }}'
  ansible.builtin.shell: '{{
      "/usr/bin/tar " ~
      ("--exclude=MediaCover" if not lidarr__cfg._backup_metadata else "") ~
      " -cvJf " ~ lidarr__archive ~ " ."
    }}'
  args:
    executable: '/bin/bash'
    chdir: '{{ lidarr___app._var_d }}'
  changed_when: true

- name:
    '{{ "Backup | fetch | " ~ lidarr__archive }}'
  ansible.posix.synchronize:
    mode: pull
    src: '{{ lidarr__archive }}'
    dest: '{{ lidarr__cfg._backup_d ~ "/" }}'

- name: 'Backup | filesystem | cleanup'
  ansible.builtin.file:
    path: '{{ "/tmp/" ~ lidarr__srv._start ~ "_config.tar.xz" }}'
    state: 'absent'

- name: 'Backup | start'
  ansible.builtin.import_tasks: 'start.yml'

- name: 'Backup | success ✔'
  ansible.builtin.meta: 'end_host'
